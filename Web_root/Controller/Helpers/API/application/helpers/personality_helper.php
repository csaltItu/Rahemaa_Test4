<?php
defined('BASEPATH') OR exit('No direct script access allowed');

if ( ! function_exists('geTotal')) {
    function getTotal()
    {
        /*log_message('error',"In helper");
        //Getting reference to the controller object
        $CI = get_instance();

        //Loading model
        $CI->load->model('Personality_model');

        $personality_total =  $CI->Personality_model->getPersonalityTotal();

        log_message('error',$personality_total );

        return $personality_total;*/
        log_message('error',"In helper");

    }
}